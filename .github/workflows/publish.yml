name: Publish Docker image

on:
  push:
  workflow_dispatch:

jobs:
  push_to_registry:
    name: Push Docker image to GitHub
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
    - name: Check out the repo
      uses: actions/checkout@v6

    - name: Log in to the Container registry
      uses: docker/login-action@v4
      with:
        registry: ghcr.io
        username: datagutten
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Log in to Docker Hub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract metadata (tags, labels) for Docker
      id: meta
      uses: docker/metadata-action@v6
      with:
        images: ghcr.io/datagutten/rfid-gate-log

    - name: Build and push backend Docker image
      uses: docker/build-push-action@v6
      with:
        push: true
        tags: |
          datagutten/rfid-gate-log:backend
          ghcr.io/datagutten/rfid-gate-log:latest
        labels: ${{ steps.meta.outputs.labels }}

    - name: Build and push nginx Docker image with static data
      uses: docker/build-push-action@v6
      with:
        context: nginx
        push: true
        tags: |
          datagutten/rfid-gate-log:nginx
          ghcr.io/datagutten/rfid-gate-log-nginx:latest
        labels: ${{ steps.meta.outputs.labels }}
